const MyRoutines = () => {
  return (
    <main>
      <h2>My Routines</h2>
      <p>Only available to logged in users:</p>
      <p>Create routine button</p>
      <p>List of created routines</p>
      <p>Ability to edit routines, add activities</p>
    </main>
  );
};

export default MyRoutines;
